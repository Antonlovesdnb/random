@version: 3.13
#@include "scl.conf"
options {
chain_hostnames(no);
create_dirs (yes);
dir_perm(0755);
dns_cache(yes);
keep_hostname(yes);
log_fifo_size(2048);
log_msg_size(8192);
perm(0644);
time_reopen (10);
use_fqdn(no);
};
source net {
 syslog(transport(udp));
};
destination remote { file("/var/log/syslog-incoming/${FULLHOST}/$HOST-$YEAR-$MONTH-$DAY.log"); };
log { source(net); destination(remote); };
